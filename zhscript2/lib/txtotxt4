模块“txtotxt库”。
赋予“句柄”【私】以函数集“‘参数0’.so”。

函数“html2text”以“html2text__”、‘句柄’、“-A-c*-i-i”（源、是不是文件、用于页面）。
（\n为<br/>，&族替换，用于处理post数据）
函数“html2html”以“html2html__”、‘句柄’、“-A-c*”（源）。

函数“iconv”     以“iconv__”、‘句柄’、“-A-c*-${l0}-Z”（源（、c*（源编码，默认gb18030）、c*（目标编码，默认utf8）、c*（x|X 输出十六进制）））。
函数“iconv_file”以“iconv__”、‘句柄’、“-A-c*-${l1}-Z”（源（、c*、c*、c*））。
