#!../zhscript2 --。
别名数以参数1。
如果不‘数’那么赋予数以16。

显示下原样
		if(fi->ret_ == func::param::Typ::Void) {
			func::VA fn = (func::VA)fi->addr_;
			switch(params.size()) {
上原样。

定义进行、头以下代码
	循环‘数’次先
		显示下原样			case 上原样‘次’下原样:
				上原样‘头’下原样fn(上原样。
		循环‘次’第先
			如果‘第’大于1那么显示“, ”。
			显示params“[”先算术‘第’-1了“]”。
		了。
		显示下原样);
				break;
上原样。了。上代码。
进行。

显示下原样			default:
				return false;
			}
		} else {
			func::UA fn = (func::UA)fi->addr_;
			switch(params.size()) {
上原样。

进行“ret3 = ”。

显示下原样			default:
				return false;
			}
		}
上原样。
