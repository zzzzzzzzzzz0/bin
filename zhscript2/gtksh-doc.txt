接口所用定义“使”、“直使”
“直使”后带窗名页名


代码、回调代码
参数0为事件名

创建、插件名（、参数……）
插件可选 webkit、vte、src、cairog
参数内被认为是系统参数的有
插件不同，前几个参数的用途不同
webkit 网址，空则about:blank
vte 命令（带参数）、所在目录、环境变量（以换行分隔的名值对，还有个“基本环境”）
src 所打开文件的路径
cairog 无（并不是蓝代码）

移动、x、y
x和y前带+号时为从当前位置算
x和y小于0时为从屏幕外倒算

移动、x、y、
限制移出屏外

移动、x、y、-1
保持数值原样？用于拖动？

移动、x、y、0代码
代码传x和y等，是计算后的数值，返回是否不使用

移动、x、y、ix、iy
ix是(屏宽-宽)/ix，iy同理

鼠标穿透
仅放在“初始”事件才起作用


插件 webkit


插件 vte
“影子程序”指若有赋值，则在命令头匹配时直接换掉，后面部分扔掉（？）
“初始命令”是插件初始后立即插入的一段命令


插件 src


插件 cairog
